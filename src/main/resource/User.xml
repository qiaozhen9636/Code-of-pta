<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test1">
    <select id="selectAll" parameterType="int" resultType="com.qiaozhen.beans.Damo1">
        select * from damo1  order by ${value};
    </select>
</mapper>